apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls-certificate
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  secretName: keycloak-tls-certificate
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  commonName: sso.massopen.cloud
